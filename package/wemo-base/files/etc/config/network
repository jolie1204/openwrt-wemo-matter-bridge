config interface 'loopback'
	option device 'lo'
	option proto 'static'
	option ipaddr '127.0.0.1'
	option netmask '255.0.0.0'

# Switch (LAN = VLAN 1 ports 0/1/2/3, WAN = VLAN 2 port 4)
config switch
	option name 'switch0'
	option reset '1'
	option enable_vlan '1'

config switch_vlan
	option device 'switch0'
	option vlan '1'
	option ports '0 1 2 3 6t'

config switch_vlan
	option device 'switch0'
	option vlan '2'
	option ports '4 6t'

# Bridge for the LAN ports
config device
	option name 'br-lan'
	option type 'bridge'
	list ports 'eth0.1'

# LAN gets its IP via DHCP (client)
config interface 'lan'
	option device 'br-lan'
	option proto 'dhcp'

# Onboarding AP (unchanged)
config interface 'setup_ap'
	option device 'ra0'
	option proto 'static'
	option ipaddr '10.22.22.1'
	option netmask '255.255.255.0'

# Wi-Fi STA uplink (unchanged)
config interface 'wwan'
	option device 'apcli0'
	option proto 'dhcp'

# Optional wired WAN on the WAN jack
config interface 'wan'
	option device 'eth0.2'
	option proto 'dhcp'

